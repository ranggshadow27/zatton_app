# Zatton App - Zabbix Traffic Capture

Aplikasi automation capture traffic router di Zabbix.

## Cara Menjalankan

1. Install dependencies:
   ```bash
   pip install customtkinter tkcalendar selenium webdriver-manager pillow pytesseract pandas pyperclip requests
   ```
